'use strict'

function init() {
    console.log('Hi')
}

function getSelector(i, j) {
    // TODO: return a selector string which can be used to 
    // select the cell in (i, j)
}

function renderCell(i, j, content) {
    // TODO: place the content parameter in cell (i, j)
}

function onNavigate(ev) {
    console.log(ev.key)
}